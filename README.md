# Waymore
A Blog Website

Add config.env with
PORT,
NODE_ENV,
JWT_SECRET,
EXPIRES_IN,
COOKIE_EXPIRES_IN

and in "db.config" file set your own MYSQL credentials to run the program

then
cd client
npm install
npm start

cd ../server
npm install
npm start

and server will run on http://localhost:{PORT}
